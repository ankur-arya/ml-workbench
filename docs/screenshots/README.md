# Screenshots

Captured from the local UI (`python -m workbench ui`) after a demo iris bakeoff.

Placeholders — replace with fresh captures from your machine:

- `home-empty.png` — first-run empty state with **Run demo experiment**
- `compare-leaderboard.png` — experiment scorecard, recommended winner, leaderboard
- `side-by-side.png` — two-run compare with metrics and confusion matrices
- `registry.png` — production champion linked to the winning run

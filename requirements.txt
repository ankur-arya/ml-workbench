# Pinned for a local ClearML + scikit-learn workbench.
# Python 3.10–3.13. Install with:  pip install -r requirements.txt
# MLflow is intentionally not installed — ClearML is the only tracker.
clearml==2.1.12
scikit-learn==1.7.2
pandas==2.2.3
numpy==2.2.6
matplotlib==3.10.3
PyYAML==6.0.2
joblib==1.5.1
pytest==8.3.5
